services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./work:/work
  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    environment:
      - BACKEND_URL=http://localhost:8080
    depends_on:
      - backend
